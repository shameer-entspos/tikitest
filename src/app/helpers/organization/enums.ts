export enum ACTIONTYPE {
  TOGGLE,
  EDIT,
  SEARCH,
  SELECT_USER,
  DESELECT_USER,
  TOGGLE_USER_SELECTION,
  VISIBLE,
}

export enum APPSTORETYPE {
  SHOWMODEL,
  TOGGLE,
  AFTERADDINGAPP,
}

export enum BILLINGTYPE {
  APPS,
  PLAN,
  LICENSE,
  INVOICE,
  UPDATE_SECTION,
}
